# Руководство по системе Whitelist и защите каналов

## 📋 Что делает система

### 1. **Защита от неавторизованного доступа**
- Автоматически проверяет всех, кто присоединяется к каналу
- Немедленно исключает пользователей без активной подписки
- Работает даже если кто-то поделился ссылкой-приглашением

### 2. **Whitelist (белый список)**
- Пользователи в whitelist **никогда не будут исключены** из канала
- Даже если у них истекла подписка, они остаются в канале
- Используется для администраторов и важных пользователей

### 3. **Автоматическая проверка подписок**
- Каждый час проверяет истекшие подписки
- Исключает пользователей с истекшей подпиской (кроме whitelist)

---

## 🔧 Админ-команды для управления Whitelist

### Добавить пользователя в whitelist

```
/whitelist_add <user_id> <channel_1|channel_2>
```

**Примеры:**
```
/whitelist_add 123456789 channel_1
/whitelist_add @username channel_2
```

**Что происходит:**
- Пользователь добавляется в whitelist для указанного канала
- Он никогда не будет исключен, даже если подписка истечет
- Можно использовать telegram_id или @username

---

### Удалить пользователя из whitelist

```
/whitelist_remove <user_id> <channel_1|channel_1>
```

**Примеры:**
```
/whitelist_remove 123456789 channel_1
/whitelist_remove @username channel_2
```

**Что происходит:**
- Пользователь удаляется из whitelist
- Теперь он будет исключен, если подписка истечет

---

### Просмотреть всех в whitelist

```
/whitelist_view [channel_name]
```

**Примеры:**
```
/whitelist_view              # Показать всех из всех каналов
/whitelist_view channel_1   # Показать только для канала 1
/whitelist_view channel_2   # Показать только для канала 2
```

**Что показывается:**
- Telegram ID пользователя
- Username (если есть)
- Канал, для которого добавлен в whitelist
- Дата добавления

---

## ⚙️ Как работает защита

### Сценарий 1: Пользователь присоединяется по ссылке

1. Пользователь нажимает на ссылку-приглашение
2. Бот **мгновенно** получает уведомление о присоединении
3. Бот проверяет:
   - ✅ Есть активная подписка? → **Разрешить**
   - ✅ В whitelist? → **Разрешить**
   - ❌ Нет подписки и не в whitelist? → **Исключить немедленно**

**Время реакции:** < 1 секунды

---

### Сценарий 2: Подписка истекла

1. Каждый час бот проверяет все подписки
2. Находит подписки с истекшей датой окончания
3. Проверяет whitelist:
   - ✅ В whitelist? → **Оставить в канале**
   - ❌ Не в whitelist? → **Исключить из канала**

---

## ⚠️ Важные требования

### Для работы системы необходимо:

1. **Бот должен быть администратором** в обоих каналах
2. **Бот должен иметь право банить пользователей**
3. **Включены обновления chat_member** (автоматически при polling)

### Проверка прав бота:

1. Откройте настройки канала
2. Перейдите в "Администраторы"
3. Найдите вашего бота (`@DemiurgAssistentbot`)
4. Убедитесь, что включено:
   - ✅ Блокировать участников
   - ✅ Добавлять участников

---

## 📝 Примеры использования

### Пример 1: Добавить администратора в whitelist

```
/whitelist_add 5882350650 channel_1
/whitelist_add 5882350650 channel_2
```

Теперь администратор никогда не будет исключен, даже без подписки.

---

### Пример 2: Проверить, кто в whitelist

```
/whitelist_view
```

Вы увидите список всех пользователей в whitelist.

---

### Пример 3: Удалить пользователя из whitelist

```
/whitelist_remove 123456789 channel_1
```

Пользователь будет удален из whitelist и может быть исключен при истечении подписки.

---

## 🔍 Логирование

Все действия логируются:

- `[CHAT_MEMBER]` - события присоединения к каналу
- `[SCHEDULER]` - проверка истекших подписок
- `✅` - успешные операции
- `❌` - ошибки

Проверяйте логи для отладки:
```bash
tail -f bot.log | grep CHAT_MEMBER
```

---

## 🛡️ Защита от обхода

Система защищает от:

1. ✅ **Поделились ссылкой** - неавторизованные пользователи исключаются сразу
2. ✅ **Истекла подписка** - автоматическое исключение каждый час
3. ✅ **Попытка обхода** - проверка при каждом присоединении

**Невозможно обойти**, если:
- Пользователь не в whitelist
- У пользователя нет активной подписки

---

## 💡 Советы

1. **Добавляйте в whitelist только доверенных пользователей**
2. **Регулярно проверяйте whitelist** командой `/whitelist_view`
3. **Используйте whitelist для администраторов и модераторов**
4. **Не добавляйте всех подряд** - это снижает безопасность

---

## ❓ Частые вопросы

**Q: Что если пользователь уже в канале, но не в whitelist?**  
A: При следующей проверке истекших подписок он будет исключен, если у него нет активной подписки.

**Q: Можно ли добавить пользователя в whitelist для обоих каналов сразу?**  
A: Нет, нужно добавлять отдельно для каждого канала:
```
/whitelist_add 123456789 channel_1
/whitelist_add 123456789 channel_2
```

**Q: Что если бот не получает события присоединения?**  
A: Убедитесь, что бот является администратором с правами на баны. Также проверьте логи на наличие ошибок.

**Q: Как проверить, работает ли защита?**  
A: Попросите пользователя без подписки присоединиться по ссылке - он должен быть исключен немедленно.

---

## 📞 Поддержка

При возникновении проблем проверьте:
1. Права бота в каналах
2. Логи бота на наличие ошибок
3. Правильность формата команд

